[package]
name = "kla-parser"
version = "0.1.0"
edition = "2021"
description = "Klingon morphological parser CLI"

[dependencies]
kla-parser-lib = { path = "../kla-parser-lib" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
